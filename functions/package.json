{
  "name": "functions",
  "version": "1.0.0",
  "description": "Neo SmartCore — push notifications engine (Firebase Cloud Functions)",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "private": true,
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.1"
  }
}