import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent inahakikisha App inasajiliwa vizuri kwenye Expo Go
// na kwenye native build (inafanana na AppRegistry.registerComponent).
registerRootComponent(App);
